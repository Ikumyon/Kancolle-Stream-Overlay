# 動作確認

すべてのテキストファイルは UTF-8 で開いて確認すること。

## 自動確認

```powershell
npm run verify
```

設定値の正規化、旧レイアウト設定の移行、疲労回復時間の計算、スクリプトの依存順序、UTF-8と文字化けを確認する。

## 手動確認

拡張機能を再読み込みして、次の順に確認する。

1. 任意の通常ページで、タイマー、情報パネル、コントロールパネルが一度だけ表示される。
2. 各パネルを移動・リサイズし、ページを再読み込みして位置とサイズが復元される。
3. タイマーの手動入力、プリセット、停止、自動表示、完了通知が動作する。
4. 疲労度の現在値と目標値から時間を計算でき、母港給糧艦を有効にすると49を超えて54まで計算できる。
5. 海域、難易度、進行状態、自由記述、リストの追加・カウント・削除が表示へ反映される。
6. 設定画面で行・列・カードを追加・削除し、列幅と内容幅を調整して保存後も復元される。
7. BGM連動を有効にするとバックグラウンドだけがSSEへ接続し、BGM名と戦果が更新される。
8. BGM連動を無効にすると接続が停止し、「連動機能無効」と表示される。
9. デフォルトへ戻す操作で、設定とレイアウトが初期状態へ戻る。
10. 日本語コメントと画面文言に文字化けがない。
